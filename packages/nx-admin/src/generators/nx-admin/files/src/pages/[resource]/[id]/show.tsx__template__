import { useResource } from '@raftlabs/nx-admin';
import React, { FC } from 'react';

const ShowViewPage = () => {
  const { show } = useResource();
  const ShowComponent: FC = show;
  return <ShowComponent />;
};

export default ShowViewPage;
