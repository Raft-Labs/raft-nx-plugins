import { useResource } from '@raftlabs/nx-admin';
import React, { FC } from 'react';

const EditViewPage = () => {
  const { edit } = useResource();
  const EditComponent: FC = edit;
  return <EditComponent />;
};

export default EditViewPage;
