import { Stack } from '@fluentui/react';
import React, { FC } from 'react';

const AuthLayout: FC = ({ children }) => {
  return (
    <Stack
      styles={{ root: { height: '98vh' } }}
      horizontalAlign="center"
      verticalAlign="center"
    >
      {children}
    </Stack>
  );
};

export default AuthLayout;
